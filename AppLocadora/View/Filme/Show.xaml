<Page x:Class="AppLocadora.View.Filme.Show"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Show">

    <Grid>
        <StackPanel>
            <Label Content="{Binding Nome}"></Label>
            <Label Content="{Binding Sinopse}"></Label>
            <Label Content="{Binding AnoLancamento}"></Label>
            <Label Content="{Binding Duracao}"></Label>
            <Label Content="{Binding Genero}"></Label>
            <Label Content="{Binding Censura}"></Label>
            <Label Content="{Binding Diretor}"></Label>
            <Label Content="{Binding Atores}"></Label>
            <Image Source="{Binding Capa}"></Image>
            <!--Falta implementar o Trailer-->
        </StackPanel> 
    </Grid>
</Page>
